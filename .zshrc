# Lines configured by zsh-newuser-install
HISTFILE=~/.histfile
HISTSIZE=1000
SAVEHIST=1000

# End of lines configured by zsh-newuser-install
# setxkbmap fr

alias com="git commit -am 'default commit'"
alias pp="com && git push"
alias lsa="ls -al"
alias prog="cd ~/Documents"
alias csharp="cd ~/Documents/CSharp"

drun firefox
